<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/border_item">

    <!-- LINEAR BAR DU HAUT -->
    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/ripple"
        android:padding="10dp">

        <!-- NOM -->
        <TextView
            android:id="@+id/tvNameEvent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/generalMargin"
            android:padding="5dp"
            android:text="WonderDay OpenAIr"
            android:textAppearance="@style/Base.TextAppearance.AppCompat.Small.Inverse"
            android:background="@drawable/rounded_corner"
            android:textSize="@dimen/size_title" />
    </LinearLayout>

    <!-- LINEAR CENTER -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="162dp"
        android:background="@drawable/ripple"
        android:orientation="horizontal">

        <!-- VISUEL EVENT-->
        <ImageView
            android:id="@+id/ivPhotoEvent"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="4dp"
            android:src="@drawable/articles"/>
    </LinearLayout>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/contest_condition"
            android:layout_margin="@dimen/generalMargin"
            android:ellipsize="marquee" />
    </LinearLayout>

    <!-- BONS PLANS -->
    <LinearLayout
        android:gravity="center"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/generalMargin"
        android:orientation="horizontal">

        <!-- CONCOURS -->
        <Button
            android:id="@+id/btnConcours"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="5dp"
            android:layout_margin="@dimen/generalMargin"
            android:padding="5dp"
            android:text="Partager sur Facebook"
            android:textAppearance="@style/Base.TextAppearance.AppCompat.Small.Inverse"
            android:background="@drawable/rounded_corner_concours"
            android:textSize="@dimen/size_title" />
    </LinearLayout>

    <!-- TRAIT SEPARATION -->
    <View
        android:layout_width="fill_parent"
        android:layout_height="1dp"
        android:background="@color/cardview_shadow_start_color" />

</LinearLayout>
